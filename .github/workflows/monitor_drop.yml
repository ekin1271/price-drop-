name: Price Drop Monitor
on:
  schedule:
    - cron: '*/45 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  monitor:
    runs-on: ubuntu-latest
    steps:
      - name: Kodu çek
        uses: actions/checkout@v4

      - name: Node.js kur
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Bağımlılıkları yükle
        run: npm install

      - name: Scripti çalıştır
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.CHAT_ID }}
        run: node price_drop_monitor.js

      - name: State dosyasını kaydet ve push et
        run: |
          git config user.name "github-actions"
          git config user.email "actions@github.com"
          git fetch origin
          git add drop_state.json
          git diff --staged --quiet || git commit -m "state güncellendi"
          git push origin main || (git pull origin main --rebase -X ours && git push origin main)
